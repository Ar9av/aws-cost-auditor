# Audit output — may contain account IDs, resource IDs, spend amounts
reports/
*.audit.json
*.audit.md

# AWS creds should NEVER be committed
.aws/
aws-credentials*
*.pem

# Local Claude settings
.claude/settings.local.json

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
